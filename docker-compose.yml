version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - VITE_API_BASE_URL=http://localhost:5000
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - PORT=5000
      - DEBUG=False
    volumes:
      - .:/app
      - /app/node_modules
